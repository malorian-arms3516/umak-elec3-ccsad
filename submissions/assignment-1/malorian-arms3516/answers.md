ANSWER_1: The log explicitly states "Permission denied", where the 'course-portal' account cannot access the portal.conf file due to insufficient privileges.
ANSWER_2: The 'course-portal' account cannot read the file due to having insufficient permissions. Its current permissions are shown as -rw------- or 400. The 'course-portal' account cannot access the portal.conf file as the group permission is set to (0). If permissions are set properly, the portal.conf file can then be accessed and edited. 
ANSWER_3: 640
ANSWER_3_WHY: 640 is the correct solution since it retains read and write privileges for root, while handing the 'course-portal' account read permission. 400 fails to solve the problem as write permission is removed from root, and group permission is still set to (0) preventing the user from accessing the portal.conf file. 755 is unsuitable as it unnecessarily grants execution access to other users on a static configuration file. It also allows other users to freely read the file, which should be kept hidden from untrusted users. 777 allows read and write access to the portal.conf file, and allows the 'course-portal' account to read the file but allows other users within the same system to do the same. They may make unwanted changes to the file as they have the same read, write, and execution permissions the 'course-portal' user has.
ANSWER_4_ORDER: B, G, E, D, F, A, I, C, H
ANSWER_5: A major risk gained from using 'chmod 777' is that all other unauthorized users on the system is able to access and make changes to the file in question. If user A under group A assigns hello.txt with 'chmod 777', user B from group B is able to make changes and edit the file, as well as remove the file entirely from the system. It allows for malicious users, code, or compromised processes to make changes or run unsigned code.
ANSWER_6: A viable proof that shows the service is back up and running is that new log entries are created in /var/log/course-portal/app.log that shows the startup was successful and no errors were encountered.
ANSWER_7_BRIDGE: component=file permissions, detect=monitoring, recover=backups, proof=conducting checks & testing